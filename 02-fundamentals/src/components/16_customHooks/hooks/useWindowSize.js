import React from 'react'

// FIXME: please implement this useWindowSize custom hook
const useWindowSize = () => {
  const [width, setWidth] = useState({ width: 0, height: 0});
  return { width: 0, height: 0}
}

export default useWindowSize